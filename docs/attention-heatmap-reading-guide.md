# 注意力热图阅读说明

> 适用：`memory_attention_heatmap` 工具的输出报告。读者：agent 自己、人类用户、Portalk 未来的用户。
> 一句话定位：**热图量的是灯，不是房间**——它记录"什么被问过"，不判断"什么重要"。

## 这张图在量什么

每次语义检索（你或系统拿一个问题去找记忆），都会在意思最近的几片街区各留一次到访记录。热图 = 窗口期内全部到访按「关键词簇 × 向量邻居区」统计的榜单。所以它的本质是**到访记录仪**，不是重要性仪表盘。

一行长这样：

```
jieba:考证 | emb:#671,674,678 ░░░░░░ 73 (0.1%) sim=0.44
```

从左到右：关键词｜这片簇住的几片向量邻居｜条形图｜7天内被点亮次数｜占总到访比例｜平均相似度。

## 四个口径，读数前先对表

1. **按行还是按次**。一次搜索会点亮几十条记忆——行数大 ≠ 被问得多。典型：每天凌晨的全库扫描（"洒水车"，见下）一次记 900+ 行。
2. **簇名口径 vs 记忆本体口径**。报告条目按簇名聚合。"簇从未被照亮"≠"记忆从未被照亮"——洒水车不挑街区，记忆本体可能天天被扫到，只是以别的路径命中（`t1_prefetch` 等），没有给那个簇名记分。
3. **时区**。日志存 UTC。看到"18 点洪峰"先加 8 换算成北京时间再判断是什么任务。
4. **_unclassified 不是垃圾场，是收货区**。没归上簇的行几乎全是前一晚刚写入的新记忆，等当晚 cluster 重建后归队。全库 920 条记忆 910 条已归簇。

## 来源分布：洒水车与真人

来源分布告诉你是谁在照亮记忆。但按行数看，一台每天全库扫描的任务会淹没所有真人对话。双口径版本（v2.5+）并排给出：

```
MCP搜索（手动）: 82903行 (97.8%) ｜ ≈101次 (16.2%)
T1语义检索（主动）: 1464行 (1.7%) ｜ ≈487次 (78.2%)
```

按行算是洒水车的世界，按次算才是检索行为的世界。同一份日志，两个口径讲两个故事——报告两个都讲，选择权在读图的人。

## 从未被照亮名单

按簇名口径列出窗口期内零命中的簇（v2.5+ 按词频降序、封顶 50 个）。读法：它不是"死区清单"，是"还没被问过的街区"。黑区 ≠ 死区；从未照亮只说明没有一次搜索以它为最佳路径，不说明记忆不存在或不重要。

## 这张图不做什么

- 不判断重要性。灯亮的次数多，只说明被问得多。
- 不反映"今天发生了什么"。它照出的是**查询者的镜子**——深夜谁拿着手电在城里走，图上就是谁的形状。
- 不做遗忘依据。热图只记录，不裁决。

## 版本记录

- v2.4：来源分布（按行单口径）
- v2.5（2026-09-06）：读图须知头部；来源分布双口径（行+次）；从未照亮名单修复——旧算法拿复合簇名和裸词比对，全库 3243/3243 簇都被列为从未照亮；按词频排序、封顶50。同日修复源于一次翻案：读者拿 7 天报告读出"某主题从未被照亮"，复查真账发现口径错位——显示层的锅不该让读者背。
